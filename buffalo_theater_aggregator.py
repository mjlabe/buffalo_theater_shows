from pandas import Dataframe

from ds9 import Scraper


def format_sites_data(scraped_data):
    pass
    return data
    
    
def main()
    scraper = Scraper(whatever_ai_settings=settings.tge_settings).
    scraped_sites_data = scraper.aggregate(
        sites=settings.sites,
    )
    df = Dataframe(formatted_sites_data(scraped_sites_data)
    st.show(df)